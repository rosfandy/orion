#!/bin/sh
# Pre-push hook: run linter before pushing

echo "Running linter before push..."
npm run lint

if [ $? -ne 0 ]; then
    echo "❌ Linter failed. Fix errors before pushing."
    exit 1
fi

echo "✅ Linter passed."
